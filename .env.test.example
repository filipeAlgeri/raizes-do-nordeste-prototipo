# Copie este arquivo para .env.test e preencha com seus valores reais.
# NUNCA commite .env.test — ele está no .gitignore.

DATABASE_URL="postgresql://USUARIO:SENHA@localhost:5432/raizes_db_test"
PORT=3001
NODE_ENV=test
JWT_SECRET="gere-com-node-e-crypto.randomBytes(64).toString('hex')"
JWT_EXPIRES_IN="1h"
PAYMENT_MOCK_MODE="always_approve"
DATA_RETENTION_DAYS=365
